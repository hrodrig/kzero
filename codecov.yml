# https://docs.codecov.com/docs/codecov-yaml
# Token is configured in GitHub: Settings → Secrets → CODECOV_TOKEN

coverage:
  status:
    project:
      default:
        target: auto
        informational: true
    patch:
      default:
        informational: true

comment: false
