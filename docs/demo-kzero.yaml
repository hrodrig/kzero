schema_version: "1.0"
# Minimal profile for docs/demo.tape (VHS). No hooks, no Helm releases — only workload refs.
pipelines:
  down:
    - deployment.demo/app
  up:
    - deployment.demo/app
retry:
  attempts: 3
  delay: 1s
run:
  mode: "dry-run"
  timeout: 10m
  operation_timeout: 30s
